{
  "name": "@solargis/dsc-mini",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "build:docs": "bun run build && bun scripts/build-docs.ts",
    "test": "bun test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@types/bun": "^1.3.13",
    "typescript": "^5.6.0",
    "vite": "^7.3.2",
    "vite-plugin-dts": "^4.5.4"
  }
}
