node_modules/
dist/
.nx/
.angular/
coverage/
tmp/

# docs/ is the published artifact directory. Everything in it is committed
# so jsDelivr can serve from the git tree (jsDelivr serves git, not Pages).
# Run `bun run build:docs` before pushing to keep the bundles current.

.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.env
.env.local
.env.*.local

.idea/
.vscode/
*.swp
